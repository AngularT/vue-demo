import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const state = {
  count: 1
};
const mutations = {
  
};
const getters = {
  
};
const actions = {
  
};

// 暴露出去
export default new Vuex.Store({
  state,mutations,getters,actions
});
